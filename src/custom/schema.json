{
  "$schema": "http://json-schema.org/schema",
  "id": "CustomServiceInit",
  "title": "bakeme custom ServiceName",
  "type": "object",
  "description": "Generate services",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the service",
      "$default": {
        "$source": "argv",
        "index": 1
      }
    }
  }
}
